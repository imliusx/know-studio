<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="know.studio.arag.evaluation.infra.persistence.mapper.EvaluationSampleMapper">
    <insert id="insertJson">
        INSERT INTO eval_samples(
            id, workspace_id, dataset_id, question, relevant_chunk_ids, expected_answer, created_at
        )
        VALUES (
            #{id}, #{workspaceId}, #{datasetId}, #{question}, CAST(#{relevantChunkIds} AS JSONB),
            #{expectedAnswer}, #{createdAt}
        )
    </insert>

    <select id="selectOwned"
            resultType="know.studio.arag.evaluation.infra.persistence.entity.EvaluationSampleEntity">
        SELECT id, workspace_id, dataset_id, question,
               relevant_chunk_ids::TEXT AS relevant_chunk_ids, expected_answer, created_at
        FROM eval_samples
        WHERE workspace_id = #{workspaceId}
          AND dataset_id = #{datasetId}
        ORDER BY created_at, id
    </select>
</mapper>
