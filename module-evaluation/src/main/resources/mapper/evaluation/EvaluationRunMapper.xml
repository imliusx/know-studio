<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="know.studio.arag.evaluation.infra.persistence.mapper.EvaluationRunMapper">
    <insert id="insertJson">
        INSERT INTO eval_runs(
            id, workspace_id, dataset_id, user_id, config, recall_at_k,
            sample_count, avg_latency_ms, extra, created_at
        )
        VALUES (
            #{id}, #{workspaceId}, #{datasetId}, #{userId}, #{config}, #{recallAtK},
            #{sampleCount}, #{avgLatencyMs}, CAST(#{extra} AS JSONB), #{createdAt}
        )
    </insert>
</mapper>
