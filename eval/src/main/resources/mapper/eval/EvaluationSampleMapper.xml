<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="know.studio.eval.infra.persistence.mapper.EvaluationSampleMapper">
    <insert id="insertJson">
        INSERT INTO eval_samples(
            id, knowledge_base_id, dataset_id, question, relevant_chunk_ids, expected_answer,
            expect_refusal, created_at
        )
        VALUES (
            #{id}, #{knowledgeBaseId}, #{datasetId}, #{question}, CAST(#{relevantChunkIds} AS JSONB),
            #{expectedAnswer}, #{expectRefusal}, #{createdAt}
        )
    </insert>

    <select id="selectOwned"
            resultType="know.studio.eval.infra.persistence.entity.EvaluationSampleEntity">
        SELECT id, knowledge_base_id, dataset_id, question,
               relevant_chunk_ids::TEXT AS relevant_chunk_ids, expected_answer,
               expect_refusal, created_at
        FROM eval_samples
        WHERE knowledge_base_id = #{knowledgeBaseId}
          AND dataset_id = #{datasetId}
        ORDER BY created_at, id
    </select>
</mapper>
